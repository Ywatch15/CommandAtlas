MONGODB_URI=mongodb://127.0.0.1:27017/commandatlas
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here
PORT=3001
CLOSED_BETA_ENABLED=false
BETA_INVITE_KEY=your_beta_invite_key_here

# Temporarily disabled during active development to prevent localhost SW caching issues across projects.
# Will be properly scoped to production builds in a later pass (to be revisited after current milestone work; do not remove sw.js or offline logic).
NEXT_PUBLIC_ENABLE_SW=false
