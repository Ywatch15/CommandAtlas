---
slug: git
name: Git
description: >
  Git version control commands for tracking changes, collaborating on code, managing
  branches, and maintaining project history. Every command in this category is invoked
  as a subcommand of the `git` binary and carries a fully-qualified slug (e.g.
  git-commit, git-clone) to guarantee global uniqueness.
parent: null
order: 2
status: published
---

Git is the version control system underlying virtually all modern software development.
Understanding not just the syntax but the mental model — commits as snapshots, branches
as lightweight pointers, the index as a staging area — is what separates effective use
from rote command-running.
