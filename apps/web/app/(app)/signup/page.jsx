import SignupPageClient from './SignupPageClient.jsx';

export const metadata = {
  title: 'Create Account — CommandAtlas',
  robots: { index: false, follow: false },
};

export default function SignupPage() {
  return <SignupPageClient />;
}
