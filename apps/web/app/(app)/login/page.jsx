import LoginPageClient from './LoginPageClient.jsx';

export const metadata = {
  title: 'Sign In — CommandAtlas',
  robots: { index: false, follow: false },
};

export default function LoginPage() {
  return <LoginPageClient />;
}
